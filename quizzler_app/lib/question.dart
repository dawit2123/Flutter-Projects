class Questions {
  String questionText = '';
  bool questionAnswer = true;
  // Questions(this.questionText, this.questionAnswer);
  Questions(q, a) {
    questionText = q;
    questionAnswer = a;
  }
}
